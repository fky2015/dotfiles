# If the network is whin CN.
export DC_CN=true
# If the server is a remote server.
export REMOTE_SERVER=true
# If use zinit to download extra binary,
# only enable this when using old distro.
export ZINIT_DOWNLOAD_BIN=false
